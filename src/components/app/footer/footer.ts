export interface CardContent {
  title: string;
  content: string;
  url: string;
}

export interface LegelLink {
  title: string;
  url: string;
}

export interface CommunityMediaIcon {
  name: string;
  type: string;
  url: string;
}

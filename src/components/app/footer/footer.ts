export interface CardContent {
  title: string;
  content: string;
  url: string;
}

export interface LegalLink {
  title: string;
  url: string;
}

export interface CommunityMediaIcon {
  name: string;
  type: string;
  url: string;
}
